{
  "name": "unfurl-discussion-comment",
  "version": "1.0.0",
  "description": "GitHub Action to unfurl links in Discussion comments",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build unfurl.mjs -o dist"
  },
  "dependencies": {
    "@octokit/graphql": "^8.0.0",
    "jsdom": "^24.0.0",
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0"
  }
}
